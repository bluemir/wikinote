roles:
  - name: admin
    rules:
    - verbs: # allow all rule
  - name: viewer
    rules:
    - verbs:
      - get
      resources:
      - kind: "page"
      - kind: "metadata"
  - name: banned
    rules: # deny all request
# group automatically bind role having same name
assigns:
- subject:
    kind: guest
  roles:
  - viewer
#- subject:
#    kind: group
#    name: viewer
#  role:
